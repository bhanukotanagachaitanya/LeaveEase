{
  "name": "employee-leave-management-system",
  "version": "2.0.0",
  "description": "LeaveEase Enterprise Employee Leave Management System (MERN Stack)",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "seed": "cd backend && npm run seed",
    "build": "cd frontend && npm run build",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "push": "git add . && git commit -m \"Update project structure with frontend and backend folders\" && git push origin main"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
